% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/10-cron_quality_build2.R
\name{build_quality_build2}
\alias{build_quality_build2}
\title{Reconstruire la table `quality_build`}
\usage{
build_quality_build2(con)
}
\arguments{
\item{con}{Connexion DBI active (PostgreSQL).}
}
\value{
Invisiblement le tibble écrit dans `quality_build`.
}
\description{
Calcule, pour chaque symbol, l'ensemble des ratios de qualité en une seule
passe, en joignant cinq sources : `financial_stmts_build` (dernière période
+ moyennes ROA/ROE 5 ans), `valuation_build` (close, buy ratios),
`cagr_stmts_build` (PEG), `cies_profile_build` (secteur, industrie).

L'écriture est faite en transaction (TRUNCATE + append) : si elle échoue,
la table existante est préservée.
}
