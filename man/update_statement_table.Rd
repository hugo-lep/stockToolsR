% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/4-cron_statements_fcts.R
\name{update_statement_table}
\alias{update_statement_table}
\title{Upsert d'une table d'états financiers dans PostgreSQL}
\usage{
update_statement_table(con, df, table_name, cols_ref, replace = FALSE)
}
\arguments{
\item{con}{Connexion DBI}

\item{df}{data.frame contenant les données à synchroniser}

\item{table_name}{Nom de la table PostgreSQL}

\item{cols_ref}{Vecteur de colonnes servant de clé logique (ex: `c("date", "symbol")`)}

\item{replace}{Logique. Si `TRUE`, remplace les lignes existantes. Défaut : `FALSE`.}
}
\value{
Invisiblement, le data.frame effectivement inséré, ou `NULL` si rien à insérer.
}
\description{
Met à jour une table PostgreSQL à partir d'un data.frame.
Les lignes sont comparées via les colonnes de référence (`cols_ref`).

- Si `replace = TRUE` : les lignes existantes correspondant au df sont
  supprimées puis toutes les lignes du df sont insérées.
- Si `replace = FALSE` : seules les nouvelles lignes (absentes de la table)
  sont ajoutées.
}
