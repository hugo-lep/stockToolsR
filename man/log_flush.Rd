% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/0-api_fcts.R
\name{log_flush}
\alias{log_flush}
\title{Écrire le résumé en DB + le détail sur S3 (fin de run)}
\usage{
log_flush(con, run_id = NULL, s3_folder = "stockToolsR/logs")
}
\arguments{
\item{con}{Connexion DBI PostgreSQL.}

\item{run_id}{Identifiant du run (ex: entier incrémental). `NULL`
pour générer un `BIGSERIAL` automatique.}

\item{s3_folder}{Dossier S3 du package (défaut `"stockToolsR/logs"`).}
}
\value{
Invisiblement `TRUE`.
}
\description{
Appelé en fin de cron :
- le détail (toutes les lignes) est sauvegardé dans un `.rds` journalier
  sur S3 (`stockToolsR/logs/YYYY-MM-DD.rds`, main_folder préfixé par s3db) ;
- une ligne de résumé est insérée dans `cron_log` (PostgreSQL).
}
\details{
La table `cron_log` est créée si elle n'existe pas.
}
