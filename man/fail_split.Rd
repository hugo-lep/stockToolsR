% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/2-cron_split_fcts.R
\name{fail_split}
\alias{fail_split}
\title{Marquer un split comme échoué}
\usage{
fail_split(con, symbol, split_date, notes = NULL)
}
\arguments{
\item{con}{Objet `DBIConnection`}

\item{symbol}{Ticker de l'action (ex: `"AAPL"`)}

\item{split_date}{Date du split (`Date` ou `character` au format `"YYYY-MM-DD"`)}

\item{notes}{Optionnel : texte libre (ex: message d'erreur).}
}
\value{
Invisiblement NULL.
}
\description{
Met à jour la ligne correspondante dans `split_log` pour indiquer que le
nettoyage des données a échoué. Le champ `status` passe à `"failed"` et
`notes` conserve le message d'erreur.
}
