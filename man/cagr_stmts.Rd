% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/7-cron_ratios_fcts.R
\name{cagr_stmts}
\alias{cagr_stmts}
\title{Calculer le CAGR des états financiers sur plusieurs horizons}
\usage{
cagr_stmts(
  con,
  symbols,
  cols = c("is_revenue", "is_ebitda", "is_netincome", "is_epsdiluted",
    "is_weightedaverageshsout", "is_weightedaverageshsoutdil", "bs_totalassets",
    "bs_totalliabilities", "cf_freecashflow", "caf")
)
}
\arguments{
\item{con}{Connexion DBI (PostgreSQL)}

\item{symbols}{Vecteur de tickers à traiter}

\item{cols}{Vecteur de colonnes à analyser. Par défaut : revenus, ebitda,
bénéfice net, EPS dilué, actions, actifs, dettes, free cash flow.}
}
\value{
Un tibble avec une ligne par symbole et les colonnes CAGR
  au format `cagr_\{n\}_\{col\}` (ex: `cagr_3_is_revenue`).
}
\description{
Pour chaque symbole, calcule le taux de croissance annuel composé (CAGR)
de colonnes sélectionnées de `financial_stmts_build` sur 1, 3, 5 et 10 ans.

Le calcul est effectué par période (`period2`) pour comparer des données
comparables (FY vs FY, Q1 vs Q1). Seule la dernière ligne par symbole
est conservée dans le résultat.
}
\examples{
\dontrun{
cagr_stmts(con, c("AAPL", "MSFT"))
}
}
