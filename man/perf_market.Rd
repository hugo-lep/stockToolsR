% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/perf_market.R
\name{perf_market}
\alias{perf_market}
\title{Performance globale du marché}
\usage{
perf_market(con, horizons = c(1, 5, 21, 63, 252), fun = mean, df = NULL)
}
\arguments{
\item{con}{Connexion DBI PostgreSQL.}

\item{horizons}{Vecteur numérique de nombre de lignes (jours de trading).}

\item{fun}{Fonction d'agrégation, une de `mean` ou `median`.}

\item{df}{Optionnel. Résultat de `perf_companies(con, horizons)`. S'il est
fourni, `con` est ignoré et on agrège directement ce dataframe (évite de
recharger les prix à chaque agrégation, ex. quand on bascule moy/médiane).}
}
\value{
Un tibble avec une ligne et une colonne par horizon, nommée
  `perf_<k>`.
}
\description{
Agrège la performance de toutes les compagnies (même poids) en une valeur
unique par horizon, selon `fun` (moyenne ou médiane). C'est l'indicateur
synthétique du marché : « +0.8 % aujourd'hui, +2.1 % sur 1 mois ».
}
