% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/2-cron_split_fcts.R
\name{confirm_split}
\alias{confirm_split}
\title{Confirmer un split après nettoyage réussi}
\usage{
confirm_split(con, symbol, split_date, notes = NULL)
}
\arguments{
\item{con}{Objet `DBIConnection`}

\item{symbol}{Ticker de l'action (ex: `"AAPL"`)}

\item{split_date}{Date du split (`Date` ou `character` au format `"YYYY-MM-DD"`)}

\item{notes}{Optionnel : texte libre pour commentaires (ex: `"Nettoyage effectué via cron"`)}
}
\value{
Invisiblement NULL.
}
\description{
Met à jour la ligne correspondante dans `split_log` pour indiquer que le
nettoyage des données a été effectué avec succès. Les champs `status`,
`confirmed_date` et `rewrite_date` sont mis à jour à la date du jour.
}
