% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/perf_sector.R
\name{perf_sector}
\alias{perf_sector}
\title{Performance agrégée par secteur}
\usage{
perf_sector(con, horizons = c(1, 5, 21, 63, 252), fun = mean, df = NULL)
}
\arguments{
\item{con}{Connexion DBI PostgreSQL.}

\item{horizons}{Vecteur numérique de nombre de lignes (jours de trading).}

\item{fun}{Fonction d'agrégation, une de `mean` ou `median`.}

\item{df}{Optionnel. Résultat de `perf_companies(con, horizons)`. S'il est
fourni, `con` est ignoré et on agrège directement ce dataframe (évite de
recharger les prix à chaque agrégation, ex. quand on bascule moy/médiane).}
}
\value{
Un tibble avec une ligne par secteur et une colonne `n_companies`
  (nombre de compagnies) plus une colonne par horizon, nommée `perf_<k>`.
}
\description{
Agrège la performance des compagnies de chaque secteur (même poids) en une
valeur par horizon, selon `fun` (moyenne ou médiane). Une compagnie sans
secteur déclaré est regroupée dans un secteur `NA`.
}
